/** Google OAuth — false면 Gmail/비밀번호 회원가입·로그인만 사용 */
export const GOOGLE_AUTH_ENABLED = false;

/** 회원가입 시 Gmail 인증번호 — false면 주소만 입력 (인증 생략) */
export const SIGNUP_EMAIL_VERIFICATION_ENABLED = false;

/** 회원가입 시 휴대폰 인증번호 — false면 번호만 입력 (인증 생략) */
export const SIGNUP_PHONE_VERIFICATION_ENABLED = false;

/** 회원가입 시 친구 추천 코드 — false면 입력란·검증 비활성 */
export const SIGNUP_REFERRAL_CODE_ENABLED = false;

/** 회원가입 시 프로필 사진 — false면 선택 (없어도 가입 가능) */
export const SIGNUP_PROFILE_PHOTO_REQUIRED = false;
